// Power Query for 02-kestrel-logistics
// Generated by scripts/export_powerbi.py. Do not edit by hand, it is overwritten.
//
// First create a parameter, because hard coding the path into every query means
// changing all of them when the repository moves:
//
//   Home > Manage Parameters > New Parameter
//   Name: DataFolder   Type: Text   Current Value: C:\Users\Administrator\Documents\Omnatix\BI Service\projects\02-kestrel-logistics\powerbi\data
//
// Then for each block below: Home > Get Data > Blank Query > Advanced Editor,
// paste, and rename the query to match the table name in the comment.

// ---- agg_trip_monthly  (505 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\agg_trip_monthly.parquet"))
in
    Source

// ---- dim_customer  (242 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_customer.parquet"))
in
    Source

// ---- dim_date  (760 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_date.parquet"))
in
    Source

// ---- dim_driver  (112 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_driver.parquet"))
in
    Source

// ---- dim_lane  (29 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_lane.parquet"))
in
    Source

// ---- dim_vehicle  (124 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_vehicle.parquet"))
in
    Source

// ---- fct_consignment  (159,977 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_consignment.parquet"))
in
    Source

// ---- fct_round_trip  (19,950 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_round_trip.parquet"))
in
    Source

// ---- fct_trip  (50,652 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_trip.parquet"))
in
    Source

// ---- mart_failed_deliveries  (10,431 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_failed_deliveries.parquet"))
in
    Source

// ---- mart_fuel_outliers  (124 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_fuel_outliers.parquet"))
in
    Source

// ---- mart_lane_economics  (50 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_lane_economics.parquet"))
in
    Source

// ---- mart_load_factor  (68 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_load_factor.parquet"))
in
    Source

// ---- mart_opportunity_bridge  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_opportunity_bridge.parquet"))
in
    Source

// ---- mart_sla_performance  (288 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_sla_performance.parquet"))
in
    Source
