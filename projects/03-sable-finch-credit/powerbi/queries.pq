// Power Query for 03-sable-finch-credit
// Generated by scripts/export_powerbi.py. Do not edit by hand, it is overwritten.
//
// First create a parameter, because hard coding the path into every query means
// changing all of them when the repository moves:
//
//   Home > Manage Parameters > New Parameter
//   Name: DataFolder   Type: Text   Current Value: C:\Users\Administrator\Documents\Omnatix\BI Service\projects\03-sable-finch-credit\powerbi\data
//
// Then for each block below: Home > Get Data > Blank Query > Advanced Editor,
// paste, and rename the query to match the table name in the comment.

// ---- agg_book_monthly  (23 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\agg_book_monthly.parquet"))
in
    Source

// ---- dim_agent  (62 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_agent.parquet"))
in
    Source

// ---- dim_branch  (9 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_branch.parquet"))
in
    Source

// ---- dim_client  (19,142 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_client.parquet"))
in
    Source

// ---- dim_date  (24 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_date.parquet"))
in
    Source

// ---- dim_loan  (19,880 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_loan.parquet"))
in
    Source

// ---- fct_loan_month  (176,156 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_loan_month.parquet"))
in
    Source

// ---- mart_affordability  (648 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_affordability.parquet"))
in
    Source

// ---- mart_branch_risk  (216 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_branch_risk.parquet"))
in
    Source

// ---- mart_collections  (4 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_collections.parquet"))
in
    Source

// ---- mart_debit_order  (12 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_debit_order.parquet"))
in
    Source

// ---- mart_exposure_bridge  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_exposure_bridge.parquet"))
in
    Source

// ---- mart_topup_masking  (852 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_topup_masking.parquet"))
in
    Source

// ---- mart_vintage  (2,691 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_vintage.parquet"))
in
    Source
