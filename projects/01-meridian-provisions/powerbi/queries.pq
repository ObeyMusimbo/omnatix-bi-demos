// Power Query for 01-meridian-provisions
// Generated by scripts/export_powerbi.py. Do not edit by hand, it is overwritten.
//
// First create a parameter, because hard coding the path into every query means
// changing all of them when the repository moves:
//
//   Home > Manage Parameters > New Parameter
//   Name: DataFolder   Type: Text   Current Value: C:\Users\Administrator\Documents\Omnatix\BI Service\projects\01-meridian-provisions\powerbi\data
//
// Then for each block below: Home > Get Data > Blank Query > Advanced Editor,
// paste, and rename the query to match the table name in the comment.

// ---- agg_product_month  (3,096 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\agg_product_month.parquet"))
in
    Source

// ---- agg_sales_monthly  (834 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\agg_sales_monthly.parquet"))
in
    Source

// ---- dim_customer  (485 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_customer.parquet"))
in
    Source

// ---- dim_date  (730 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_date.parquet"))
in
    Source

// ---- dim_product  (131 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_product.parquet"))
in
    Source

// ---- fct_inventory_snapshot  (82,137 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_inventory_snapshot.parquet"))
in
    Source

// ---- fct_sales_line  (282,650 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_sales_line.parquet"))
in
    Source

// ---- mart_dead_stock  (393 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_dead_stock.parquet"))
in
    Source

// ---- mart_discount_trend  (9,114 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_discount_trend.parquet"))
in
    Source

// ---- mart_gp_bridge  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_gp_bridge.parquet"))
in
    Source

// ---- mart_margin_waterfall  (238 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_margin_waterfall.parquet"))
in
    Source

// ---- mart_promo_performance  (2,558 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_promo_performance.parquet"))
in
    Source

// ---- mart_stockout_impact  (108 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_stockout_impact.parquet"))
in
    Source
