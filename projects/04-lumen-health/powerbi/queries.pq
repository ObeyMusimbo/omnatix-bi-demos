// Power Query for 04-lumen-health
// Generated by scripts/export_powerbi.py. Do not edit by hand, it is overwritten.
//
// First create a parameter, because hard coding the path into every query means
// changing all of them when the repository moves:
//
//   Home > Manage Parameters > New Parameter
//   Name: DataFolder   Type: Text   Current Value: C:\Users\Administrator\Documents\Omnatix\BI Service\projects\04-lumen-health\powerbi\data
//
// Then for each block below: Home > Get Data > Blank Query > Advanced Editor,
// paste, and rename the query to match the table name in the comment.

// ---- agg_monthly  (24 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\agg_monthly.parquet"))
in
    Source

// ---- dim_clinic  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_clinic.parquet"))
in
    Source

// ---- dim_date  (729 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_date.parquet"))
in
    Source

// ---- dim_practitioner  (32 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_practitioner.parquet"))
in
    Source

// ---- dim_scheme  (7 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\dim_scheme.parquet"))
in
    Source

// ---- fct_appointment  (232,558 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_appointment.parquet"))
in
    Source

// ---- fct_capacity_hour  (140,101 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_capacity_hour.parquet"))
in
    Source

// ---- fct_session  (16,062 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\fct_session.parquet"))
in
    Source

// ---- mart_capacity_cost  (50 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_capacity_cost.parquet"))
in
    Source

// ---- mart_claim_recovery  (42 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_claim_recovery.parquet"))
in
    Source

// ---- mart_clinic  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_clinic.parquet"))
in
    Source

// ---- mart_collection_bridge  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_collection_bridge.parquet"))
in
    Source

// ---- mart_data_quality  (7 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_data_quality.parquet"))
in
    Source

// ---- mart_no_show  (10 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_no_show.parquet"))
in
    Source

// ---- mart_patient_charges  (12 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_patient_charges.parquet"))
in
    Source

// ---- mart_practitioner  (32 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_practitioner.parquet"))
in
    Source

// ---- mart_scheme  (6 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_scheme.parquet"))
in
    Source

// ---- mart_slot_grid  (50 rows)
let
    Source = Parquet.Document(File.Contents(DataFolder & "\mart_slot_grid.parquet"))
in
    Source
